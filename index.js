console.log("Hello world");


window.addEventListener("keypress",(keynumber)=>{
    console.log(keynumber);
    if(keynumber.code=== "Enter"){ //If the enter key is pressed
        //takes the input of start and end location of the two lcoations
        //and bring user to a new page
        
        //take the two station name and fin the quickest path between them
        const startStation = document.getElementById("start").textContent;
        const endStation = document.getElementById("end").textContent;


    }
});